{% extends "base.html.twig" %}
{% block title %} Fichier update html twig{% endblock %}

{% block body %}
    <style>
    table {
    width: 100%;
    max-width: 600px;
    margin: 20px auto;
    text-align: center;
    font-size: 110%;
    border-collapse: collapse;
}

th, td {
    padding: 12px 8px;
    border-bottom: 1px solid #e2e8f0;
}

th {
    background-color: #f8fafc;
    font-weight: bold;
}

ul {
    display: flex;
    list-style-type: none;
    justify-content: flex-end;
    padding: 0;
}

a {
    display: inline-block;
    padding: 10px 16px;
    background-color: green;
    color: white;
    text-decoration: none;
    border-radius: 10px;
}

.btn-delete {
    background-color: #ef4444;
    color: #ffffff;
    font-weight: 600;
    font-size: 14px;
    padding: 8px 16px;
    border: none;
    border-radius: 6px;
    cursor: pointer;
    box-shadow: 0 2px 4px rgba(239, 68, 68, 0.2);
    transition: background-color 0.2s ease, transform 0.1s ease, box-shadow 0.2s ease;
}

.btn-delete:hover {
    background-color: #dc2626;
    box-shadow: 0 4px 6px rgba(239, 68, 68, 0.3);
}

.btn-delete:active {
    transform: scale(0.97);
}
    </style>
    <h1> Page update </h1>

 {% if app.current_route  == 'app_livre_update'%}
   <p> vous ete bien sur la route  {{ app.current_route }}</p>
   <p> {{ absolute_url(path(app.current_route,{id: form.vars.data.id } )) }}</p>
 {% endif %}  


{{ dump(tableau) }}


             {{  include ('_partials/montest.html.twig',{titre:'un super titre',auteur:'stephen king'})  }}  



                    {% set mavariable = 1000 %}
                        <p>{{ mavariable }}</p>
                    {% set monTableau  = [1,2,3,4,5] %}
                        <p> {{ monTableau|length }}</p>    
                         
                    {% set tabasc = {'clef' : 'valeur'} %}
                          <p> {{ tabasc.clef }}</p>  
                            
                    



                {% if form|length > 0 %}
                    <p> le formulaire n'est pas vide </p>
                {% else %}
                    <p> le formulaire est vide </p>
                {% endif  %}        

                {% if tableau|length > 0  %}
                  <p> le tableau est valide</p>

                        {% for data in tableau %}
                                {# <p> {{ data }}</p> #}
                         {% endfor %}
                {% else %}
                 <p> le tableau n'est pas valide</p>
                 {% endif %}  



    {{ form_start(form) }}
        {{ form_errors(form) }}
    <div class="boite">
        <div>
            {{ form_label(form.titre) }}
            {{ form_widget(form.titre) }}
            {{ form_errors(form.titre) }}
        </div>    
        <div>
            {{ form_label(form.date) }}
            {{ form_widget(form.date) }}
            {{ form_errors(form.date) }}
        </div>
      <div>
            {{ form_label(form.prix) }}
            {{ form_widget(form.prix) }}
            {{ form_errors(form.prix) }}
        </div>
       <button type="submit"> Modiffier </button>  
    </div>    
   
    {{ form_end(form) }}
 

    <a href="{{ path('app_livre') }}"> retour page livre </a>
    
{% endblock %}


{% block javascripts %}
    <script>
        console.log("hello world");
    </script>
{% endblock %}
