{% extends 'base.html.twig' %}

{% block title %}Image html twig{% endblock %}

{% block body %}
<style>
    .btn{
        text-decoration:none;
        padding: 10px 15px ;
        color :white;
        border-radius:6px;
    }
    .btn_primary{
        background:blue;
    }

    .boite{
        display:flex ;
        flex-direction:column;
        gap:20px;
    }
</style>
<div class="boite"> 
<div class="example-wrapper">
    <h1>Index image htm twig ✅</h1>

                {{ form_start(form) }}
                        {{ form_widget(form) }}

                        <button type="submit"> Envoyer</button>
                {{ form_end(form) }}
</div>
<div > 
<a class="btn btn_primary" href="{{ path('app_image_show') }}">Retour</a>
</div>
</div>
{% endblock %}
